# Key packages for ShoppingGenRec Inference on AMD MI300X (ROCm 7.0)
# Generated from actual working environment on 2026-03-30
# Note: torch/vllm/flash_attn/triton are pre-installed from the Docker image
#       DO NOT upgrade torch — vllm C++ extensions are ABI-locked to 2.9.1
#       setuptools downgraded to 79.0.0 for vllm compat (<80)

# === Inference Engine ===
vllm==0.16.0rc2.dev293+g2b84ac669.rocm700  # pre-installed from Docker image; requires transformers<5
transformers==4.57.6           # must be <5 for vllm compat; does NOT support Qwen3.5 natively
                               # (vllm has its own model loading, so Qwen3.5 inference works)

# === Model & Tokenizer ===
huggingface_hub==0.36.2
tokenizers==0.22.2
safetensors==0.7.0
sentencepiece==0.2.1
peft==0.18.1

# === Torch stack (pre-installed from Docker image, DO NOT pip install) ===
# torch==2.9.1+git8907517     # custom ROCm build; vllm compiled against this version
# torchaudio==2.9.0+eaa9e4e
# torchvision==0.24.1+d801a34
# triton==3.4.0               # ROCm triton
# triton_kernels==1.0.0
# flash_attn==2.8.3           # ROCm flash attention
# NOTE: DO NOT upgrade torch — vllm C++ extensions are ABI-locked to 2.9.1

# === ROCm-specific (pre-installed from Docker image) ===
# amd-aiter==0.1.10.post2
# amdsmi==26.0.0+37d158ab
# mori==0.1.dev185+g2d02c6a98

# === vLLM Dependencies ===
accelerate==1.12.0
compressed-tensors==0.13.0
openai==2.21.0                # for OpenAI-compatible API serving
ray==2.54.0                   # distributed inference
xgrammar==0.1.29              # structured output / grammar-guided decoding
outlines_core==0.2.11
lm-format-enforcer==0.11.3
gguf==0.17.1
tiktoken==0.12.0
mistral_common==1.9.1

# === Data Processing ===
datasets==4.5.0
numpy==2.2.6
pandas==3.0.0
pyarrow==23.0.1

# === API & Networking ===
fastapi==0.129.0
uvicorn==0.41.0
aiohttp==3.13.3
httpx==0.28.1
requests==2.32.5
pydantic==2.12.5
grpcio==1.78.0

# === Azure / Cloud ===
azure-storage-blob==12.19.0
azure-identity==1.25.2
azureml-mlflow==1.61.0.post1

# === Monitoring & Logging ===
prometheus-fastapi-instrumentator==7.1.0
prometheus_client==0.24.1
loguru==0.7.3

# === Misc ===
pillow==12.1.1
opencv-python-headless==4.13.0.92
timm==1.0.24
einops==0.8.2
PyYAML==6.0.3
tqdm==4.67.3
setuptools==79.0.0             # downgraded from 82.0.0 for vllm compat (<80)
